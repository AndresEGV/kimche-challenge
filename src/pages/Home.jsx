import React, { useState } from "react";
import SearchBar from "../components/SearchBar";

const Home = () => {
  return (
    <div>
      <p>Search conuntry</p>
      <SearchBar />
    </div>
  );
};

export default Home;
